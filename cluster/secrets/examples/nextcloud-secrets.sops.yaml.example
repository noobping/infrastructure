apiVersion: v1
kind: Secret
metadata:
  name: nextcloud-secrets
  namespace: nextcloud
type: Opaque
stringData:
  # Generate independently with: openssl rand -base64 48
  postgres-password: REPLACE_BEFORE_ENCRYPTING

