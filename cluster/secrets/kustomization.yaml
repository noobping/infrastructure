apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Add only SOPS-encrypted files here. The examples/ directory is deliberately
# excluded so placeholder credentials can never be reconciled by Flux.
resources: []

