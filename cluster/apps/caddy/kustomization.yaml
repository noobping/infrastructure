apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ingress
resources:
  - workload.yaml
  - network-policy.yaml
configMapGenerator:
  - name: caddy-config
    files:
      - Caddyfile
generatorOptions:
  disableNameSuffixHash: true

