apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-postgresql
  labels:
    app.kubernetes.io/name: paperless-postgresql
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: paperless-postgresql
  labels:
    app.kubernetes.io/name: paperless-postgresql
spec:
  serviceName: paperless-postgresql
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: paperless-postgresql
  template:
    metadata:
      labels:
        app.kubernetes.io/name: paperless-postgresql
    spec:
      automountServiceAccountToken: false
      containers:
        - name: postgresql
          image: docker.io/library/postgres:18.3-alpine@sha256:54451ecb8ab38c24c3ec123f2fd501303a3a1856a5c66e98cecf2460d5e1e9d7
          imagePullPolicy: IfNotPresent
          env:
            - {name: POSTGRES_DB, value: paperless}
            - {name: POSTGRES_USER, value: paperless}
            - {name: POSTGRES_PASSWORD_FILE, value: /run/secrets/paperless/db-password}
          ports:
            - {name: postgresql, containerPort: 5432, protocol: TCP}
          readinessProbe:
            exec:
              command: [pg_isready, -U, paperless, -d, paperless]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: [pg_isready, -U, paperless, -d, paperless]
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests: {cpu: 200m, memory: 512Mi}
            limits: {cpu: "2", memory: 2Gi}
          volumeMounts:
            - {name: data, mountPath: /var/lib/postgresql}
            - {name: secrets, mountPath: /run/secrets/paperless, readOnly: true}
      terminationGracePeriodSeconds: 60
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: paperless-postgresql
        - name: secrets
          secret:
            secretName: paperless-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: paperless-postgresql
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: paperless-postgresql
  ports:
    - {name: postgresql, port: 5432, targetPort: postgresql}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-redis
  labels:
    app.kubernetes.io/name: paperless-redis
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: paperless-redis
  labels:
    app.kubernetes.io/name: paperless-redis
spec:
  serviceName: paperless-redis
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: paperless-redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: paperless-redis
    spec:
      automountServiceAccountToken: false
      containers:
        - name: redis
          image: docker.io/library/redis:8.8.0-alpine@sha256:9d317178eceac8454a2284a9e6df2466b93c745529947f0cd42a0fa9609d7005
          imagePullPolicy: IfNotPresent
          args: [redis-server, --appendonly, "yes", --save, "60", "1"]
          ports:
            - {name: redis, containerPort: 6379, protocol: TCP}
          readinessProbe:
            exec:
              command: [redis-cli, ping]
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            exec:
              command: [redis-cli, ping]
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests: {cpu: 50m, memory: 128Mi}
            limits: {cpu: "1", memory: 512Mi}
          volumeMounts:
            - {name: data, mountPath: /data}
      terminationGracePeriodSeconds: 30
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: paperless-redis
---
apiVersion: v1
kind: Service
metadata:
  name: paperless-redis
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: paperless-redis
  ports:
    - {name: redis, port: 6379, targetPort: redis}

