# Seed only a missing tracked reference. systemd-tmpfiles never overwrites a
# reviewed immutable tag already selected by the administrator.
C /etc/bootc-image-ref 0644 root root - /usr/lib/infrastructure/nas-bootc-image-ref
