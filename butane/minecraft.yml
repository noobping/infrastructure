passwd:
  users:
    - name: minecraft
      uid: 1003
      home_dir: /var/lib/containers/minecraft
      shell: /usr/bin/nologin

storage:
  files:
    - path: /etc/hostname
      overwrite: true
      contents:
        inline: |
          minecraft.vm
