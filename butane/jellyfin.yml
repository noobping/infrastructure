passwd:
  users:
    - name: music
      uid: 1001
      home_dir: /var/lib/containers/jellyfin
      shell: /usr/bin/nologin

storage:
  files:
    - path: /etc/hostname
      overwrite: true
      contents:
        inline: |
          jellyfin.vm
